# Managed by Ansible (ops-platform, school.yml). Adds to the unit from the app
# repo without editing it: upload after a successful local backup, alert on a
# failed one.
[Unit]
OnSuccess=dlz-offsite.service
OnFailure=dlz-alert@%N.service
